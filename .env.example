# Database Configuration
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=wordpress
DB_ROOT_PASSWORD=rootpassword
DB_HOST=db

# WordPress Configuration
WP_PORT=8080
WP_DEBUG=1

# WordPress Site URL (optional, will use container name if not set)
WP_SITE_URL=http://localhost:8080
